cheats.hits.nuttertools = function() {
    var weapons = [11, 12, 28, 29, 30, 31, 32, 33];
    for (var i = 0; i < weapons.length; i++) {
        localPlayer.giveWeapon(weapons[i], 300, false);
    }
};
