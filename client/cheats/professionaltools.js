cheats.hits.professionaltools = function() {
    var weapons = [18, 20, 23, 25, 26, 28];
    for (var i = 0; i < weapons.length; i++) {
        localPlayer.giveWeapon(weapons[i], 200, false);
    }
};
